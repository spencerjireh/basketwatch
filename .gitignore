.env
.env.*
!.env.example
node_modules/
dist/
.turbo/
.next/
*.tsbuildinfo
__pycache__/
.venv/
scratch/
docker-compose.local.yml
.playwright-mcp/
.DS_Store
*.log
.obsidian/

# Regenerated by next dev/build, and its contents differ between the two, so
# tracking it produces permanent churn. Next recreates it on every build.
next-env.d.ts

# Screenshots and scratch images. Four dotplot PNGs reached main because a
# `git add -A` swept the repo root; nothing here is worth versioning, and the
# ones that are belong in docs/ with a name that says what they show.
*.png
!apps/web/**/*.png
!docs/**/*.png
.claude/worktrees/
.pnpm-store/
lab/
